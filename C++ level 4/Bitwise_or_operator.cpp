#include <iostream>
using namespace std;

int main()
{

    /*The | (bitwise OR) in C or C++ takes two numbers as operands and does OR on every bit of two numbers. The result of OR is 1 if any of the two bits is 1. */
    cout << "Result: " << (12 | 25) << endl; // 29

    return 0;
}